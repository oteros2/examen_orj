import 'package:flutter/material.dart';

class ListViewScreenOrj extends StatelessWidget {
   
  const ListViewScreenOrj({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('ListViewScreenOrj'),
      ),
    );
  }
}