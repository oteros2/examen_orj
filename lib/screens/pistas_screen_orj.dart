import 'package:flutter/material.dart';

class PistasScreenOrj extends StatelessWidget {
   
  const PistasScreenOrj({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('PistasScreenOrj'),
      ),
    );
  }
}